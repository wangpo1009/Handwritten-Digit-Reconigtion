# Dependencies dành riêng cho data/model monitoring.
# Cài trong .venv-monitor để tránh package "multipart" của Litestar
# xung đột với "python-multipart" của Gradio/FastAPI trong .venv.

evidently>=0.7,<0.8
numpy>=1.24,<2.3
pandas>=2.1,<2.3
scipy>=1.10,<1.14
scikit-learn>=1.3,<1.6
pyyaml>=6.0,<7

# Dùng để chạy tests/test_monitoring độc lập.
pytest>=8.0,<8.3
pytest-cov>=4.1,<5.1
